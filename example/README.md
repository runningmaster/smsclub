This is a small example of using that package in CLI app.

```
USAGE:
  -cmd string (balance | send | status)
  -id string, ...
  -sender string
  -text string
  -to string, ...
  -token string
  -user string

```